// arrays, objetos
let meta = {
    value: 'Ler um livro por mês',
    numero: 1,
    checked: false,
    log: (info) => {
        console.log(info);
    }
}

meta.value = "Não é mais para ler um livro";
meta.log(meta.value)

// function
// const criarMeta = () => {}

// function criarMeta() {}